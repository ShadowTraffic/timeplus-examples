{
    "name": "heap-readings",
    "image": "shadowtraffic/shadowtraffic:latest",
    "postAttachCommand": "source free-trial-license.env && java -jar /home/shadowtraffic.jar --config /workspaces/timeplus-examples/heap-readings.json --sample 10 --stdout --watch",
    "customizations": {
        "codespaces": {
            "openFiles": [
                "heap-readings.json"
            ]
        }
    }
}
